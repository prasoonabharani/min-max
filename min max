class Compute 
{
    static pair getMinMax(long a[], long n)  
    {pair p;
    long min=a[0];
    long max=-1;
    for (int i=0;i<a.length;i++)
    {
    min=Math.min(a[i],min);
     max=Math.max(a[i],max);   //Write your code here
    }
    p=new pair (min,max);
    return p;
}
}
